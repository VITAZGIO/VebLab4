info:
  name: Create Task
  type: http
  seq: 7

http:
  method: POST
  url: http://127.0.0.1:4200/tasks
  body:
    type: json
    data: |-
      {
        "title": "Первая задача",
        "description": "Проверка защищенного CRUD после авторизации",
        "status": "new"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
