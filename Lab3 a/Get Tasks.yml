info:
  name: Get Tasks
  type: http
  seq: 8

http:
  method: GET
  url: http://127.0.0.1:4200/tasks?page=1&limit=10
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
